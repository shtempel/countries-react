import React, { FC } from 'react';

interface CountryCardProps {

}

export const CountryCard: FC<CountryCardProps> = (props: CountryCardProps) => {
    return (
        <div>CountryCard</div>
    );
};
